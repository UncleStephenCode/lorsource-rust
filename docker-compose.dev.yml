services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lor
      POSTGRES_USER: lor
      POSTGRES_PASSWORD: lor
    ports:
      - "5432:5432"
    volumes:
      - lor_pgdata:/var/lib/postgresql/data
volumes:
  lor_pgdata:
